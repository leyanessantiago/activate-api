export enum VerificationLevel {
  UNVERIFIED,
  CODE_VERIFIED,
  USER_INFO_ADDED,
  INTERESTS_ADDED,
}

export enum RelationshipStatus {
  PENDING,
  ACCEPTED,
  DECLINED,
  BLOCKED,
}

export enum FollowerStatus {
  FOLLOWING,
  MUTED,
  BLOCKED,
}
